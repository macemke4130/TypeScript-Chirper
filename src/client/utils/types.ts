export interface Chirp {
    id: number,
    user: string,
    msg: string
  }

const dummyFunction = () => {
    // Modal needs a function passed in. This exists to placate TS --
}

export default dummyFunction;